@model PendingStudentWorkLinkViewModel?

@if (Model is not null)
{
    <a href="@Model.ViewUrl" target="_blank" rel="noopener" title="@Model.FileName">
        v@(Model.VersionNumber)
    </a>
}
else
{
    <span class="text-muted">—</span>
}
